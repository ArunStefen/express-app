machine:
   node:
    version: 4.0.0

deployment:
  develop:
    branch: develop
    commands:
      - echo "Deploying from develop..."
  master:
    branch: master
    commands:
      - echo "Copying package because merging to master..."
      - sh beanstalk/createVersion.sh `git describe --abbrev=0 --tags`
